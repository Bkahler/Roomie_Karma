<%@current_household.members.each do |x|%>
  <% if x.id ==params[:id].to_i%>
  <%= x.name%>
  <%= x.email%>
  <%= x.score%>
  <%=link_to "delete", member_path(x.id), :method=> :delete, :confirm => "sure ?" %>
  <%=link_to "edit",edit_member_path(x.id), :method=> :get%>
  <%end%>
<%end%>

