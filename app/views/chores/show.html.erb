<%@current_household.chores.each do |x|%>
  <% if x.id == params[:id].to_i %>
    <%= x.name %>
    <%=link_to "delete", chore_path(x.id), :method=> :delete, :confirm=> "sure ?" %>
    <%=link_to "edit", edit_chore_path(x.id), :method=> :get%>
  <%end%>
<%end%>